<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>User Profile</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Pacifico&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    body {
      /* background-color: #f7f3f9; */
      /* background: linear-gradient(90deg, #ec407a, #f06292); */
      background-color: #fff0f5;
    }

    .profile-card {
      max-width: 500px;
      margin: 60px auto;
      padding: 30px;
      background-color: white;
      border-radius: 20px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
    }

    .profile-header {
      text-align: center;
      margin-bottom: 25px;
    }

    .profile-detail {
      margin-bottom: 15px;
      font-size: 1.1rem;
    }

    .btn-edit {
      background-color: #ff66a3;
      color: white;
    }

    .btn-edit:hover {
      background-color: #ff3385;
    }
  </style>
</head>

<body>

  <div class="container">
    <% if (success && success.length > 0) { %>
      <div class="alert alert-success alert-dismissible fade show mt-3" role="alert">
        <%= success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>

    <div class="profile-card">
      <div class="profile-header">
        <h2>User Profile</h2>
      </div>

      <div class="profile-detail"><i class="fas fa-user me-2"></i> <strong>Name:</strong> <%= user.username %></div>
      <div class="profile-detail"><i class="fas fa-envelope me-2"></i> <strong>Email:</strong> <%= user.email %></div>

      <div class="text-center mt-4">
        <a href="/user" class="btn btn-edit me-2"> ← Back To List</a> 

        <a href="/user/change-password" class="btn btn-outline-secondary"><i class="fas fa-key me-1"></i>Change Password</a>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>