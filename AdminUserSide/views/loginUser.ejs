<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Sign In - PinkTheme</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to right, #ffe6f0, #ffc0cb);
      font-family: 'Segoe UI', sans-serif;
    }
    .login-container {
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .login-box {
      background-color: #fff;
      padding: 40px;
      border-radius: 20px;
      box-shadow: 0 0 20px rgba(255, 20, 147, 0.2);
      width: 100%;
      max-width: 420px;
    }
    .login-box h2 {
      text-align: center;
      color: #d63384;
      font-weight: 700;
      margin-bottom: 30px;
    }
    .form-control {
      border-radius: 10px;
      border: 2px solid #ffc0cb;
    }
    .form-control:focus {
      border-color: #ff69b4;
      box-shadow: 0 0 0 0.2rem rgba(255, 105, 180, 0.3);
    }
    .btn-pink {
      background-color: #ff69b4;
      color: white;
      font-weight: 500;
      border-radius: 25px;
      transition: 0.3s;
    }
    .btn-pink:hover {
      background-color: #ff1493;
    }
    .google-btn {
      background: white;
      border: 2px solid #e0e0e0;
      border-radius: 30px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 500;
      margin-bottom: 20px;
      padding: 10px;
      color: #333;
      transition: 0.3s;
    }
    .google-btn:hover {
      background-color: #f8f8f8;
    }
    .google-btn i {
      margin-right: 8px;
      font-size: 18px;
    }
    .forgot-link {
      float: right;
    }
    .footer-text {
      text-align: center;
      margin-top: 15px;
    }
    .footer-text a {
      color: #d63384;
    }
  </style>
</head>
<body>
  
  <div class="login-container">
    <div class="login-box">

      <h2>Sign in to Dribbble</h2>

      <% if (typeof messages !== 'undefined' && messages.error && messages.error.length > 0) { %>
        <div class="alert alert-danger text-center">
          <%= messages.error[0] %>
        </div>
      <% } %>

      <!-- Google Sign-In Button -->
      <a href="#" class="google-btn">
        <i class="fab fa-google"></i> Sign in with Google
      </a>

      <div class="text-center my-3 text-muted">or sign in with email</div>

      <form action="/user/loginUser" method="POST">
        <div class="mb-3">
          <label class="form-label">Username or Email</label>
          <input type="email" class="form-control" name="email" required placeholder="Enter your email">
        </div>

        <div class="mb-3">
          <label class="form-label d-flex justify-content-between">
            <span>Password</span>
            <a href="/user/forgot-password" class="forgot-link">Forgot?</a>
          </label>
          <input type="password" class="form-control" name="password" required placeholder="Enter your password">
        </div>

        <button type="submit" class="btn btn-pink w-100 py-2 mt-2">Sign In</button>
      </form>

      <p class="footer-text mt-4">Don't have an account? <a href="/user/register">Sign up</a></p>
    </div>
  </div>
</body>
</html>
